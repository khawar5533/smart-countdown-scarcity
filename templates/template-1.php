
<div class="wbgs-banner">
    <a href="{{permalink}}">
        <h2>{{title}}</h2>
        <img src="{{image_url}}" alt="{{title}}">
    </a>
    <p>Sale Price111: {{sale_price}}</p>
    <p>Stock Remainingeee: {{stock}}</p>
    <p>{{custom_text}}</p>
    <div id="{{countdown_id}}" class="wbgacountdown" data-end-time="{{end_time}}"></div>
</div>